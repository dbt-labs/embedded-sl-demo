# dbt Semantic Layer for embedded analytics

## Running

To run in "production" mode, do
```
docker compose up
```

To run in "development" mode, with hot-reloads and `debugpy`, do
```
docker compose -f dev.compose.yaml up
```
