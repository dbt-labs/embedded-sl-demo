version: "3"

services:
  server:
    build:
      context: .
      target: dev 
    ports:
      - "8080:80"
      - "5678:5678"
    env_file: .env
    volumes:
      - type: bind
        source: src/
        target: /usr/app/src
        read_only: true

